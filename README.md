First DO ## `npm install` then do ## `npm install axios` to get full node_modules

## Available Scripts

This is a simple CRUD app with react as front end, coldfusion as back end. This repo dosen't contain node_modules and axios in it. So I request you to do  -- npm install -- and -- npm install axios-- then -- npm start--to run this react app

In the project directory, you can run:

### `npm start`

